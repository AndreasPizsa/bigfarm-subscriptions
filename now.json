{
  "version": 2,
  "builds": [
    { "src": "dist/**/*", "use": "@now/static-build" }
  ],
  "routes": [
    { "src": "/subscriptionStatusChange", "dest": "/dist/index.html" },
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
